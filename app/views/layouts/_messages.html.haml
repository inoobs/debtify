- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{ class: "alert alert-#{message_type(name.to_s)}" }
      %button.close{ 'aria-hidden' => 'true', 'data-dismiss' => 'alert', type: 'button' } &times;
      = content_tag :div, msg, id: "flash_#{name}"
